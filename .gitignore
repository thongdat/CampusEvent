target/
.idea/
*.iml
*.log
*.class
.mvn/wrapper/maven-wrapper.jar

# Ảnh người dùng tải lên (runtime, không commit)
uploads/

# Local secret / OAuth / SMTP configuration (KHÔNG commit credential thật)
src/main/resources/application-local.properties
application-local.properties
.env
.env.local

# Word temp / lock files
docs/~$*

# Doc generator dependencies (npm install in docs/.wordgen)
docs/.wordgen/node_modules/
